import { Text, View } from "react-native";
import { FC } from "react";

const HomeScreen: FC = () => {
  return (
    <View>
        <Text> Instruction</Text>
      <Text>
        Select the plus icon in the top right corner 2. Insert
        Product Name, Best Used by, Owner & if you'd like to change your
        notification. Select Add 3. Now your product is successfully entered &
        if you'd like to add to your Grocery List select the icon on
      </Text>
    </View>
  );
};

export default HomeScreen;
